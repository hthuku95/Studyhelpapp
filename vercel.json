{
    "builds": [{
        "src": "studyhelp/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "500mb", "runtime": "python3.10" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "studyhelp/wsgi.py"
        }
    ]
}